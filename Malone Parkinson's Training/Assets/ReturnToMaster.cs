using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;
using UnityEngine.VR;

public class ReturnToMaster : MonoBehaviour {

    //public Button returnToMasterButton;

    //public LevelManager myLevelManager;

    ////private GameObject controller;

    //void Start()
    //{
    //    myLevelManager = GameObject.Find("Game Manager").GetComponent<LevelManager>();
    //    Button btn = returnToMasterButton.GetComponent<Button>();
    //    btn.onClick.AddListener(TaskOnClick);
    //}

   

    //void TaskOnClick()
    //{
    //    myLevelManager.ReturnMasterScene();
    //    SceneManager.UnloadSceneAsync("End Scene");
    //    myLevelManager.curLevel = null;
    //    myLevelManager.needToUnloadScene = false;

    //    //needs to change the value of current level from end scene back to "none?"

    //    GameObject.Find("Game Manager").GetComponent<GameSettings>().curFPC.SetActive(true);

    //    //hide the cursor once more
    //    GameObject.Find("Game Manager").GetComponent<HideCursor>().Hide();

    //}
}
